@model HospitalTeam2.Models.ViewModels.AlertEdit

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Alert</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form action="/Alert/Create" method="post">
            <div class="form-group">
                <label class="control-label">Alert Title</label>
                <input class="form-control" type="text" name="AlertTitle" />
            </div>
            <div class="form-group">
                <label class="control-label">Alert Message</label>
                <input class="form-control" type="text" name="AlertMessage" />
            </div>
            <div class="form-group">
                <input class="btn btn-default" type="submit" value="submit" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
